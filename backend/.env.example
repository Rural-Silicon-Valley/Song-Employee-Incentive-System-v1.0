# Prisma / DB
DATABASE_URL=file:./dev.db

# Auth
JWT_SECRET=replace_with_32char_secret________________
TOKEN_EXPIRES_IN=1d
TOKEN_PREFIX=GV

# Mail (choose one provider)
# Option A: Resend (recommended for production/testing)
MAIL_PROVIDER=resend
RESEND_API_KEY=your_resend_api_key
MAIL_FROM=no-reply@yourdomain.com

# Option B: SMTP (recommended for local/corporate)
# MAIL_PROVIDER=smtp
# SMTP_HOST=localhost
# SMTP_PORT=1025
# SMTP_USER=
# SMTP_PASS=
# MAIL_FROM=no-reply@yourdomain.com
